# FastMCP - The MCP server framework
fastmcp>=0.1.0

# Database dependencies
sqlalchemy>=2.0.0
sqlite3  # Built-in Python module

# PDF parsing dependencies
PyPDF2>=3.0.0
pdfplumber>=0.9.0
pymupdf>=1.23.0  # Alternative PDF parser (fitz)

# Data extraction and processing
regex>=2023.0.0
python-dateutil>=2.8.0  # For date parsing
decimal>=1.70  # For precise currency calculations

# Text processing and pattern matching
re2>=1.0.0  # Fast regex engine
fuzzywuzzy>=0.18.0  # For fuzzy string matching
python-levenshtein>=0.21.0  # For string similarity

# Configuration and environment
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Logging and monitoring
loguru>=0.7.0

# Development and testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.0.0 